<p id="notice"><%= notice %></p>

<h1>Books</h1>
<h3 class="text-center">books for Sale</h3>

<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <% @books.each do |book| %>
      <%= render "book_row", book: book, order_item: @order_item %>
    <% end %>
  </div>
</div>


<!--
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Year</th>
      <th>Isbn</th>
      <th>Region</th>
      <th>Rating</th>
      <th>Price</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.year %></td>
        <td><%= book.isbn %></td>
        <td><%= book.region %></td>
        <td><%= book.rating %></td>
        <td><%= book.price %></td>
        <td><%= book.image %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %> -->
